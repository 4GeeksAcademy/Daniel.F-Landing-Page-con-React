import React from "react";

const Card = () => {
  return (

    <div className="card">
      <h3>{title}</h3>
      <p>{description}</p>
      <button>{buttonText}</button>
    </div>

  );
};

      export default Card;